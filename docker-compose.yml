services:
  backend:
    image: python:3.12-slim
    working_dir: /app
    volumes: ["./backend:/app"]
    ports: ["8000:8000"]
    command: sh -c "pip install -q -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port 8000"
  frontend:
    image: node:22-alpine
    working_dir: /app
    volumes: ["./frontend:/app"]
    ports: ["5173:5173"]
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
