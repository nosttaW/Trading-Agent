import {useState} from 'react'; import {createRoot} from 'react-dom/client'; import './style.css';
const API='/api';
const strategy={name:'SPY moving-average crossover',version:1,hypothesis:'Medium-term trend persistence may exceed modeled transaction costs.',market:'US_EQUITIES',instrument:'SPY',timeframe:'1d',entry:{type:'moving_average_cross',fast_period:20,slow_period:100,direction:'above'},exit:{type:'moving_average_cross',fast_period:20,slow_period:100,direction:'below'},position:{type:'fixed_fraction',fraction:'0.05'},risk:{max_order_notional:'100.00',max_position_notional:'500.00',max_daily_loss:'25.00',max_drawdown_percent:'5.00',stop_loss_percent:'2.00'},schedule:{regular_hours_only:true,timezone:'America/New_York'}};
type Created={strategy_id:string;strategy_hash:string};
function App(){const [created,setCreated]=useState<Created>(); const [result,setResult]=useState<Record<string,string|number>>(); const [message,setMessage]=useState(''); const [expiry,setExpiry]=useState(''); const [typed,setTyped]=useState(''); const [account,setAccount]=useState('demo-paper-account');
 const request=async(path:string,body?:unknown)=>{const r=await fetch(API+path,{method:body?'POST':'GET',headers:{'content-type':'application/json'},body:body?JSON.stringify(body):undefined}); const x=await r.json(); if(!r.ok)throw new Error(x.detail||'Request failed'); return x};
 const create=async()=>{try{const x=await request('/strategies',strategy);setCreated(x);setResult(undefined);setMessage('Strategy locked as a declarative candidate. Run deterministic demo backtest.')}catch(e){setMessage(String(e))}};
 const test=async()=>{if(!created)return;try{const x=await request(`/strategies/${created.strategy_id}/backtest`,{});setResult(x.result);setMessage(x.disclaimer)}catch(e){setMessage(String(e))}};
 const approve=async()=>{if(!created||!expiry)return;try{const x=await request('/approvals',{strategy_id:created.strategy_id,account_id:account,mode:'paper',expires_at:expiry,typed_approval:typed});setMessage(`Paper approval active until ${x.expires_at}. Broker order submission stays blocked until a verified adapter exists.`)}catch(e){setMessage(String(e))}};
 const required=created&&expiry?`APPROVE_AUTONOMOUS ${created.strategy_id} 1 ${account} 500.00 ${new Date(expiry).toISOString()}`:'';
 return <main><header><b>Guardrail Trading</b><span>DEMO · PAPER ONLY</span></header><section className="warning"><strong>Risk notice</strong><br/>Backtests, AI research, and paper trading do not predict future returns. Execution can differ materially. Loss of all allocated capital remains possible.</section><h1>Start with one transparent strategy</h1><p className="muted">No AI key. No broker credentials. Explicit deterministic demo dataset.</p><section><h2>1. Review candidate</h2><pre>{JSON.stringify(strategy,null,2)}</pre><button onClick={create}>Lock candidate</button>{created&&<p>Strategy ID: <code>{created.strategy_id}</code><br/>SHA-256: <code>{created.strategy_hash}</code></p>}</section>{created&&<section><h2>2. Test chronologically</h2><button onClick={test}>Run demo backtest</button>{result&&<dl>{Object.entries(result).filter(([k])=>k!=='warnings').map(([k,v])=><><dt>{k.replaceAll('_',' ')}</dt><dd>{String(v)}</dd></>)}<dt>Warnings</dt><dd>{String(result.warnings).split(',').map(w=><div>{w}</div>)}</dd></dl>}</section>}{created&&result&&<section><h2>3. Approve paper execution</h2><p>Live trading unavailable. No orders are simulated.</p><label>Paper account <input value={account} onChange={e=>setAccount(e.target.value)}/></label><label>Approval expires (UTC) <input type="datetime-local" value={expiry} onChange={e=>setExpiry(e.target.value)}/></label><p>Type exactly:</p><code className="wrap">{required}</code><label>Typed approval <input value={typed} onChange={e=>setTyped(e.target.value)}/></label><button disabled={!expiry||typed!==required} onClick={approve}>Grant paper approval</button></section>}<output>{message}</output><footer>Research proposals require deterministic validation, holdout testing, paper-forward results, broker reconciliation, independent risk validation, then human approval. AI never enters the order path.</footer></main>};createRoot(document.getElementById('root')!).render(<App/>);
